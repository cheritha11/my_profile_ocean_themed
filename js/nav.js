/* ============================================================
   nav.js — now empty, logic moved to main.js
   Kept so index.html script tag doesn't 404
   ============================================================ */
